// Set the bold font for the selected ConceptVO

console.clear();
console.show();

var theConceptVO;
theConceptVO = $(selection).first();

if( !theConceptVO.view || !theConceptVO.concept){
    console.log("!! select at least one Concept VO in the current view");
    exit();
}

$(selection).each( c =>{ 
    if(c.fontStyle == "normal") c.setFontStyle("bold");
    if(c.fontStyle == "bold") c.setFontStyle("normal");    
})